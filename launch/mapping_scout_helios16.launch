<launch>
  <arg name="rviz" default="false" />

  <rosparam command="load" file="$(find faster_lio)/config/scout_helios16.yaml" />

  <param name="runtime_pos_log_enable" type="bool" value="true" />
  <node pkg="faster_lio" type="run_mapping_online" name="laserMapping" output="screen" />
  <!-- Visualize assumed lidar pose on the IMU body (same axes, z=0.307). -->
  <node pkg="tf" type="static_transform_publisher" name="body_to_rslidar"
        args="0 0 0.307 0 0 0 body rslidar 50" />

  <group if="$(arg rviz)">
    <node launch-prefix="nice" pkg="rviz" type="rviz" name="rviz"
          args="-d $(find faster_lio)/config/rviz/scout_helios16.rviz" />
  </group>
</launch>
